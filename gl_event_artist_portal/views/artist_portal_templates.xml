<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="artist_portal_page" name="Künstler- und Agenturportal">
        <t t-call="website.layout">
            <t t-set="no_header" t-value="True"/>
            <t t-set="no_footer" t-value="True"/>
            <main class="gl-ap-page">
                <div class="gl-ap-shell">
                    <header class="gl-ap-header">
                        <div class="gl-ap-eyebrow">GROUNDLIFT · KÜNSTLER &amp; AGENTUR</div>
                        <h1 t-esc="event.name"/>
                        <div class="gl-ap-event-meta">
                            <span t-if="event.date_begin">
                                <span t-field="event.date_begin" t-options="{'widget': 'datetime'}"/>
                            </span>
                            <span t-if="event.address_id and event.address_id.name"> · <t t-esc="event.address_id.name"/></span>
                        </div>
                    </header>

                    <t t-if="not portal_active">
                        <section class="gl-ap-panel gl-ap-closed">
                            <h2>Portal derzeit nicht aktiv</h2>
                            <p t-if="extended_portal">Das Portal ist ab „Gebucht“ für die Vorbereitung und ab „Abrechnung“ für den GEMA-Link erreichbar.</p>
                            <p t-else="">Gästeliste ab „Angekündigt“, GEMA-Link ab „Abrechnung“.</p>
                        </section>
                    </t>

                    <t t-else="">
                        <t t-if="success == 'added'">
                            <div class="gl-ap-alert gl-ap-alert-success">Der Eintrag wurde gespeichert und ist sofort in der Groundlift-Gästeliste berücksichtigt.</div>
                        </t>
                        <t t-if="success == 'updated'">
                            <div class="gl-ap-alert gl-ap-alert-success">Der Eintrag wurde geändert. Die aktualisierte Anzahl und Ticketkategorie sind sofort berücksichtigt.</div>
                        </t>
                        <t t-if="success == 'cancelled'">
                            <div class="gl-ap-alert gl-ap-alert-success">Der Eintrag wurde storniert. Die reservierten Plätze sind wieder freigegeben.</div>
                        </t>
                        <t t-if="error">
                            <div class="gl-ap-alert gl-ap-alert-error" t-esc="error"/>
                        </t>

                        <t t-if="accounting_active">
                            <section class="gl-ap-panel" id="gema">
                                <div class="gl-ap-kicker">ABRECHNUNG</div>
                                <h2>GEMA</h2>
                                <p class="gl-ap-help">Hier findet ihr den von Groundlift hinterlegten GEMA-Link zur Veranstaltung.</p>
                                <a t-if="gema_url" class="gl-ap-submit gl-ap-gema-link" t-att-href="gema_url"
                                   target="_blank" rel="noopener noreferrer">GEMA-Link öffnen ↗</a>
                                <p t-else="" class="gl-ap-help">Der GEMA-Link wurde noch nicht hinterlegt. Bitte wendet euch bei Fragen an Groundlift.</p>
                            </section>
                        </t>
                        <t t-if="media_active">
                        <t t-if="media_notice">
                            <div class="gl-ap-alert gl-ap-alert-success">Deine Angaben wurden gespeichert.</div>
                        </t>
                        <t t-if="show_tech or show_hospitality">
                        <section class="gl-ap-panel" id="documents">
                            <div class="gl-ap-kicker">DOKUMENTE</div><h2>Rider &amp; Bühnenanweisung</h2>
                            <p class="gl-ap-help">PDF, DOC oder DOCX · höchstens 20 MB je Datei. Eine neue Datei ersetzt den bisherigen Rider desselben Typs.</p>
                            <div class="gl-ap-media-grid">
                                <form t-if="show_tech" class="gl-ap-form" method="post" enctype="multipart/form-data" t-att-action="'/event/artist/%s/%s/rider' % (event.id, token)">
                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                    <input type="hidden" name="rider_kind" value="tech"/>
                                    <h3>Techrider / Bühnenanweisung</h3>
                                    <p t-if="rider_tech_available and event.x_studio_tech_rider" class="gl-ap-done">✓ Dokument liegt Groundlift vor</p>
                                    <label t-if="rider_tech_available"><span>Datei auswählen</span><input type="file" name="rider" accept=".pdf,.doc,.docx" required="required"/></label>
                                    <button t-if="rider_tech_available" type="submit" class="gl-ap-submit">Techrider hochladen</button>
                                    <p t-if="not rider_tech_available" class="gl-ap-help">Das zugehörige Odoo-Feld ist noch nicht eingerichtet.</p>
                                </form>
                                <form t-if="show_hospitality" class="gl-ap-form" method="post" enctype="multipart/form-data" t-att-action="'/event/artist/%s/%s/rider' % (event.id, token)">
                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                    <input type="hidden" name="rider_kind" value="hospitality"/>
                                    <h3>Hospitality Rider</h3>
                                    <p t-if="rider_hospitality_available and event.x_studio_hospitality_rider" class="gl-ap-done">✓ Dokument liegt Groundlift vor</p>
                                    <label t-if="rider_hospitality_available"><span>Datei auswählen</span><input type="file" name="rider" accept=".pdf,.doc,.docx" required="required"/></label>
                                    <button t-if="rider_hospitality_available" type="submit" class="gl-ap-submit">Hospitality Rider hochladen</button>
                                    <p t-if="not rider_hospitality_available" class="gl-ap-help">Das zugehörige Odoo-Feld ist noch nicht eingerichtet.</p>
                                </form>
                            </div>
                        </section>
                        </t>
                        <section t-if="show_press" class="gl-ap-panel" id="press">
                            <div class="gl-ap-kicker">PRESSE</div><h2>Pressetexte</h2>
                            <p class="gl-ap-help">Sobald Groundlift einen Text im Backend verändert, wird das jeweilige Textfeld hier gesperrt. Der lange Pressetext ist zunächst leer – eine eventuell vorhandene Standard-Veranstaltungsbeschreibung wird nicht übernommen.</p>
                            <form class="gl-ap-form" method="post" t-att-action="'/event/artist/%s/%s/press' % (event.id, token)">
                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                <label><span>Pressetext kurz</span>
                                    <textarea name="press_short" rows="4" maxlength="6000" t-att-disabled="event.artist_portal_short_locked and 'disabled' or None"><t t-esc="press_short_value"/></textarea>
                                </label>
                                <p t-if="event.artist_portal_short_locked" class="gl-ap-done">✓ Kurztext durch Groundlift bearbeitet – gesperrt</p>
                                <label><span>Pressetext lang / Veranstaltungsbeschreibung</span>
                                    <textarea name="press_long" rows="9" maxlength="50000" t-att-disabled="event.artist_portal_long_locked and 'disabled' or None"><t t-esc="press_long_value"/></textarea>
                                </label>
                                <p t-if="event.artist_portal_long_locked" class="gl-ap-done">✓ Langtext durch Groundlift bearbeitet – gesperrt</p>
                                <button t-if="not event.artist_portal_short_locked or not event.artist_portal_long_locked" class="gl-ap-submit" type="submit">Pressetexte speichern</button>
                            </form>
                        </section>
                        <section t-if="show_photos" class="gl-ap-panel" id="photos">
                            <div class="gl-ap-kicker">PRESSEBILDER</div><h2>Fotos hochladen</h2>
                            <p class="gl-ap-help">Mehrere Bilder pro Format sind möglich (max. 20 je Format, je max. 12 MB). JPG, PNG oder WEBP. Das erste quadratische Bild erscheint als Odoo-Veranstaltungsbild. Nach Bearbeitung in der Grafik-App ist der Fotobereich gesperrt.</p>
                            <t t-if="photo_editable">
                                <form class="gl-ap-form" method="post" enctype="multipart/form-data" t-att-action="'/event/artist/%s/%s/photos' % (event.id, token)">
                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                    <div class="gl-ap-media-grid gl-ap-media-grid-three">
                                        <label><span>Foto 1 · 1:1 quadratisch</span><input type="file" name="photo_square" accept="image/jpeg,image/png,image/webp" multiple="multiple"/></label>
                                        <label><span>Foto 2 · Querformat</span><input type="file" name="photo_landscape" accept="image/jpeg,image/png,image/webp" multiple="multiple"/></label>
                                        <label><span>Foto 3 · Hochformat</span><input type="file" name="photo_portrait" accept="image/jpeg,image/png,image/webp" multiple="multiple"/></label>
                                    </div>
                                    <button class="gl-ap-submit" type="submit">Fotos hochladen</button>
                                </form>
                            </t>
                            <p t-else="" class="gl-ap-done">✓ Groundlift hat die Fotos übernommen – weitere Änderungen sind gesperrt.</p>
                            <div class="gl-ap-photo-grid" t-if="event.artist_portal_photo_ids.filtered('active')">
                                <t t-foreach="event.artist_portal_photo_ids.filtered('active')" t-as="photo">
                                    <div class="gl-ap-photo-card">
                                        <img loading="lazy" t-att-src="'/event/artist/%s/%s/photo/%s' % (event.id, token, photo.id)" t-att-alt="photo.filename"/>
                                        <div><strong t-esc="{'square':'1:1 quadratisch','landscape':'Querformat','portrait':'Hochformat'}.get(photo.format)"/><br/><small t-esc="photo.filename"/></div>
                                        <form t-if="photo_editable" method="post" t-att-action="'/event/artist/%s/%s/photo/%s/delete' % (event.id, token, photo.id)">
                                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                            <button class="gl-ap-secondary" type="submit">Entfernen</button>
                                        </form>
                                    </div>
                                </t>
                            </div>
                        </section>
                        <t t-if="not guestlist_active">
                            <section class="gl-ap-panel"><h2>Gästeliste und Ticketstände</h2><p class="gl-ap-help">Diese Funktionen werden ab der Phase „Angekündigt“ freigeschaltet.</p></section>
                        </t>
                        </t>
                        <t t-if="guestlist_active">
                        <section class="gl-ap-stats-grid">
                            <div class="gl-ap-stat">
                                <div class="gl-ap-stat-value" t-esc="total_sold"/>
                                <div class="gl-ap-stat-label">Tickets verkauft</div>
                            </div>
                            <div class="gl-ap-stat">
                                <div class="gl-ap-stat-value">
                                    <t t-if="overall_remaining is None">∞</t>
                                    <t t-else="" t-esc="overall_remaining"/>
                                </div>
                                <div class="gl-ap-stat-label">noch verfügbar</div>
                            </div>
                            <div class="gl-ap-stat">
                                <div class="gl-ap-stat-value" t-esc="total_portal_guestlist"/>
                                <div class="gl-ap-stat-label">eure Gästeliste</div>
                            </div>
                            <div class="gl-ap-stat">
                                <div class="gl-ap-stat-value" t-esc="total_portal_box_office"/>
                                <div class="gl-ap-stat-label">eure Abendkasse</div>
                            </div>
                        </section>

                        <section class="gl-ap-panel">
                            <div class="gl-ap-section-head">
                                <div>
                                    <div class="gl-ap-kicker">LIVE TICKETSTAND</div>
                                    <h2>Verkauf &amp; Verfügbarkeit</h2>
                                </div>
                            </div>
                            <div class="gl-ap-table-wrap">
                                <table class="gl-ap-table">
                                    <thead>
                                        <tr>
                                            <th>Ticketkategorie</th>
                                            <th>Preis</th>
                                            <th>Verkauft</th>
                                            <th>Reserviert*</th>
                                            <th>Noch verfügbar</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <t t-if="not ticket_rows">
                                            <tr><td colspan="5">Für diese Veranstaltung sind keine regulären Ticketkategorien angelegt.</td></tr>
                                        </t>
                                        <t t-foreach="ticket_rows" t-as="row">
                                            <tr>
                                                <td><strong t-esc="row['ticket'].name"/></td>
                                                <td t-esc="row['price_label'] or '–'"/>
                                                <td t-esc="row['sold']"/>
                                                <td t-esc="row['guestlist_qty']"/>
                                                <td>
                                                    <t t-if="row['remaining'] is None">unbegrenzt</t>
                                                    <t t-else="" t-esc="row['remaining']"/>
                                                </td>
                                            </tr>
                                        </t>
                                    </tbody>
                                </table>
                            </div>
                            <p class="gl-ap-footnote">* Reserviert umfasst verbindliche Gästelisten- und Abendkassenplätze dieser Ticketkategorie und reduziert die verfügbare Kapazität.</p>
                        </section>

                        <section class="gl-ap-panel">
                            <div class="gl-ap-section-head">
                                <div>
                                    <div class="gl-ap-kicker">EINTRAG</div>
                                    <h2>Gästeliste oder Abendkasse</h2>
                                </div>
                            </div>
                            <form class="gl-ap-form" method="post" t-att-action="'/event/artist/%s/%s/add' % (event.id, token)">
                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                                <div class="gl-ap-type-grid">
                                    <label class="gl-ap-type-card">
                                        <input type="radio" name="entry_type" value="guestlist"
                                               t-att-checked="(form.get('entry_type') or 'guestlist') == 'guestlist' and 'checked' or None"/>
                                        <span>
                                            <strong>Gästeliste</strong>
                                            <small>Kostenloser Platz, wird direkt von der verfügbaren Kapazität abgezogen.</small>
                                        </span>
                                    </label>
                                    <label class="gl-ap-type-card">
                                        <input type="radio" name="entry_type" value="box_office"
                                               t-att-checked="form.get('entry_type') == 'box_office' and 'checked' or None"/>
                                        <span>
                                            <strong>Ticket · Abendkasse</strong>
                                            <small>Ticket wird reserviert und erst am Veranstaltungsabend an der Abendkasse bezahlt.</small>
                                        </span>
                                    </label>
                                </div>

                                <div class="gl-ap-form-grid">
                                    <label>
                                        <span>Name des Gastes *</span>
                                        <input type="text" name="name" required="required" maxlength="160" t-att-value="form.get('name', '')" placeholder="Vor- und Nachname"/>
                                    </label>
                                    <label>
                                        <span>Anzahl *</span>
                                        <select name="quantity" required="required">
                                            <t t-foreach="quantity_values" t-as="qty">
                                                <option t-att-value="qty" t-att-selected="str(qty) == form.get('quantity', '1') and 'selected' or None" t-esc="qty"/>
                                            </t>
                                        </select>
                                    </label>
                                </div>

                                <div class="gl-ap-form-grid gl-ap-box-office-field">
                                    <label class="gl-ap-full">
                                        <span>Ticketkategorie für Abendkasse *</span>
                                        <select name="price_option_id">
                                            <option value="">Bitte auswählen</option>
                                            <t t-foreach="box_office_rows" t-as="row">
                                                <option t-att-value="row['option'].id"
                                                        t-att-selected="str(row['option'].id) == form.get('price_option_id', '') and 'selected' or None"
                                                        t-esc="row['label']"/>
                                            </t>
                                        </select>
                                    </label>
                                </div>

                                <div class="gl-ap-form-grid">
                                    <label>
                                        <span>Kontaktdaten</span>
                                        <input type="text" name="contact_data" maxlength="250" t-att-value="form.get('contact_data', '')" placeholder="E-Mail oder Mobilnummer"/>
                                    </label>
                                    <label>
                                        <span>Eingetragen von</span>
                                        <input type="text" name="submitted_by" maxlength="160" t-att-value="form.get('submitted_by', '')" placeholder="Künstler, Agentur oder Ansprechpartner"/>
                                    </label>
                                </div>

                                <label class="gl-ap-full">
                                    <span>Bemerkung</span>
                                    <textarea name="note" maxlength="1500" rows="3" placeholder="Optional, z. B. +1, Presse, Abholung …"><t t-esc="form.get('note', '')"/></textarea>
                                </label>

                                <button type="submit" class="gl-ap-submit">Eintrag verbindlich speichern</button>
                            </form>
                        </section>

                        <section class="gl-ap-panel" t-if="portal_lines">
                            <div class="gl-ap-section-head">
                                <div>
                                    <div class="gl-ap-kicker">BEREITS EINGETRAGEN</div>
                                    <h2>Eure Einträge über dieses Portal</h2>
                                </div>
                            </div>
                            <div class="gl-ap-entry-list">
                                <t t-foreach="portal_lines" t-as="line">
                                    <div class="gl-ap-entry">
                                        <div class="gl-ap-entry-top">
                                            <div class="gl-ap-entry-content">
                                                <strong t-esc="line.name"/>
                                                <div class="gl-ap-entry-meta">
                                                    <span t-esc="'%s Person(en)' % line.quantity_int"/>
                                                    <span t-if="line.artist_portal_entry_type == 'guestlist'">Gästeliste</span>
                                                    <span t-if="line.artist_portal_entry_type == 'box_office'">Abendkasse</span>
                                                    <span t-if="line.price_option_id and line.artist_portal_entry_type == 'box_office'"
                                                          t-esc="option_labels.get(line.price_option_id.id, line.price_option_id.name)"/>
                                                    <span t-if="line.artist_portal_submitted_by">von <t t-esc="line.artist_portal_submitted_by"/></span>
                                                </div>
                                                <div class="gl-ap-entry-note" t-if="line.contact_data">
                                                    Kontakt: <t t-esc="line.contact_data"/>
                                                </div>
                                                <div class="gl-ap-entry-note" t-if="line.note" t-esc="line.note"/>
                                            </div>
                                            <div class="gl-ap-entry-actions">
                                                <button type="button"
                                                        class="gl-ap-secondary gl-ap-edit-toggle"
                                                        t-att-data-target="'gl-ap-edit-%s' % line.id">
                                                    Ändern
                                                </button>
                                                <form method="post"
                                                      class="gl-ap-cancel-form"
                                                      t-att-action="'/event/artist/%s/%s/cancel/%s' % (event.id, token, line.id)"
                                                      onsubmit="return confirm('Diesen Eintrag wirklich stornieren? Die reservierten Plätze werden sofort wieder freigegeben.');">
                                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                    <button type="submit" class="gl-ap-danger">Stornieren</button>
                                                </form>
                                            </div>
                                        </div>

                                        <div t-att-id="'gl-ap-edit-%s' % line.id"
                                             t-attf-class="gl-ap-edit-panel #{'is-open' if edit_line_id == line.id else ''}">
                                            <div class="gl-ap-edit-title">Eintrag ändern</div>
                                            <form class="gl-ap-form gl-ap-edit-form"
                                                  method="post"
                                                  t-att-action="'/event/artist/%s/%s/update/%s' % (event.id, token, line.id)">
                                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                                                <div class="gl-ap-type-grid">
                                                    <label class="gl-ap-type-card">
                                                        <input type="radio"
                                                               name="entry_type"
                                                               value="guestlist"
                                                               t-att-checked="((edit_form.get('entry_type') if edit_line_id == line.id else line.artist_portal_entry_type) == 'guestlist') and 'checked' or None"/>
                                                        <span>
                                                            <strong>Gästeliste</strong>
                                                            <small>Kostenloser Platz.</small>
                                                        </span>
                                                    </label>
                                                    <label class="gl-ap-type-card">
                                                        <input type="radio"
                                                               name="entry_type"
                                                               value="box_office"
                                                               t-att-checked="((edit_form.get('entry_type') if edit_line_id == line.id else line.artist_portal_entry_type) == 'box_office') and 'checked' or None"/>
                                                        <span>
                                                            <strong>Ticket · Abendkasse</strong>
                                                            <small>Reserviert, Zahlung am Veranstaltungsabend.</small>
                                                        </span>
                                                    </label>
                                                </div>

                                                <div class="gl-ap-form-grid">
                                                    <label>
                                                        <span>Name des Gastes *</span>
                                                        <input type="text"
                                                               name="name"
                                                               required="required"
                                                               maxlength="160"
                                                               t-att-value="edit_form.get('name', '') if edit_line_id == line.id else line.name"/>
                                                    </label>
                                                    <label>
                                                        <span>Anzahl *</span>
                                                        <select name="quantity" required="required">
                                                            <t t-foreach="quantity_values" t-as="qty">
                                                                <option t-att-value="qty"
                                                                        t-att-selected="str(qty) == (edit_form.get('quantity', '1') if edit_line_id == line.id else str(line.quantity_int)) and 'selected' or None"
                                                                        t-esc="qty"/>
                                                            </t>
                                                        </select>
                                                    </label>
                                                </div>

                                                <div class="gl-ap-form-grid gl-ap-box-office-field">
                                                    <label class="gl-ap-full">
                                                        <span>Ticketkategorie für Abendkasse *</span>
                                                        <select name="price_option_id">
                                                            <option value="">Bitte auswählen</option>
                                                            <t t-foreach="box_office_rows" t-as="row">
                                                                <option t-att-value="row['option'].id"
                                                                        t-att-selected="str(row['option'].id) == (edit_form.get('price_option_id', '') if edit_line_id == line.id else str(line.price_option_id.id or '')) and 'selected' or None"
                                                                        t-esc="row['label']"/>
                                                            </t>
                                                        </select>
                                                    </label>
                                                </div>

                                                <div class="gl-ap-form-grid">
                                                    <label>
                                                        <span>Kontaktdaten</span>
                                                        <input type="text"
                                                               name="contact_data"
                                                               maxlength="250"
                                                               t-att-value="edit_form.get('contact_data', '') if edit_line_id == line.id else (line.contact_data or '')"/>
                                                    </label>
                                                    <label>
                                                        <span>Eingetragen von</span>
                                                        <input type="text"
                                                               name="submitted_by"
                                                               maxlength="160"
                                                               t-att-value="edit_form.get('submitted_by', '') if edit_line_id == line.id else (line.artist_portal_submitted_by or '')"/>
                                                    </label>
                                                </div>

                                                <label class="gl-ap-full">
                                                    <span>Bemerkung</span>
                                                    <textarea name="note" maxlength="1500" rows="3"><t t-esc="edit_form.get('note', '') if edit_line_id == line.id else (line.note or '')"/></textarea>
                                                </label>

                                                <div class="gl-ap-edit-buttons">
                                                    <button type="submit" class="gl-ap-submit">Änderungen speichern</button>
                                                    <button type="button"
                                                            class="gl-ap-secondary gl-ap-edit-close"
                                                            t-att-data-target="'gl-ap-edit-%s' % line.id">
                                                        Abbrechen
                                                    </button>
                                                </div>
                                            </form>
                                        </div>
                                    </div>
                                </t>
                            </div>
                        </section>
                        </t>
                    </t>
                </div>
            </main>

            <script type="text/javascript">
                document.addEventListener('DOMContentLoaded', function () {
                    function syncFormType(formEl) {
                        if (!formEl) {
                            return;
                        }
                        const selected = formEl.querySelector('input[name="entry_type"]:checked');
                        const boxOffice = selected &amp;&amp; selected.value === 'box_office';
                        const boxOfficeField = formEl.querySelector('.gl-ap-box-office-field');
                        const boxOfficeSelect = boxOfficeField ? boxOfficeField.querySelector('select[name="price_option_id"]') : null;
                        if (boxOfficeField) {
                            boxOfficeField.style.display = boxOffice ? '' : 'none';
                        }
                        if (boxOfficeSelect) {
                            boxOfficeSelect.required = !!boxOffice;
                        }
                    }

                    document.querySelectorAll('.gl-ap-form').forEach(function (formEl) {
                        formEl.querySelectorAll('input[name="entry_type"]').forEach(function (radio) {
                            radio.addEventListener('change', function () {
                                syncFormType(formEl);
                            });
                        });
                        syncFormType(formEl);
                    });

                    document.querySelectorAll('.gl-ap-edit-toggle').forEach(function (button) {
                        button.addEventListener('click', function () {
                            const target = document.getElementById(button.dataset.target);
                            if (!target) {
                                return;
                            }
                            const willOpen = !target.classList.contains('is-open');
                            document.querySelectorAll('.gl-ap-edit-panel.is-open').forEach(function (panel) {
                                panel.classList.remove('is-open');
                            });
                            if (willOpen) {
                                target.classList.add('is-open');
                                syncFormType(target.querySelector('.gl-ap-form'));
                            }
                        });
                    });

                    document.querySelectorAll('.gl-ap-edit-close').forEach(function (button) {
                        button.addEventListener('click', function () {
                            const target = document.getElementById(button.dataset.target);
                            if (target) {
                                target.classList.remove('is-open');
                            }
                        });
                    });

                    const openEdit = document.querySelector('.gl-ap-edit-panel.is-open');
                    if (openEdit) {
                        syncFormType(openEdit.querySelector('.gl-ap-form'));
                        openEdit.scrollIntoView({behavior: 'smooth', block: 'center'});
                    }
                });
            </script>
        </t>
    </template>
</odoo>
