<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="login" name="Medienfreigabe Login">
        <t t-call="website.layout">
            <div id="wrap" class="glma-page">
                <div class="container py-5">
                    <div class="glma-login-card mx-auto">
                        <h1>Medienfreigabe</h1>
                        <p class="text-muted">Bitte gib deine persönliche PIN ein.</p>
                        <t t-if="error">
                            <div class="alert alert-danger">Die PIN wurde nicht gefunden.</div>
                        </t>
                        <form method="post" action="/media-approval/login" class="mt-4">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <div class="mb-3">
                                <input name="pin" type="password" inputmode="numeric" pattern="[0-9]*" class="form-control form-control-lg text-center" placeholder="PIN" autofocus="autofocus"/>
                            </div>
                            <button type="submit" class="btn btn-primary btn-lg w-100">Öffnen</button>
                        </form>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="folder_list" name="Medienfreigabe Ordnerliste">
        <t t-call="website.layout">
            <div id="wrap" class="glma-page">
                <div class="container py-4">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <div>
                            <h1 class="mb-1">Medienfreigabe</h1>
                            <div class="text-muted">Angemeldet als <strong t-esc="person.name"/></div>
                        </div>
                        <a href="/media-approval/logout" class="btn btn-outline-secondary">Abmelden</a>
                    </div>
                    <t t-if="not folders">
                        <div class="alert alert-info">Aktuell sind keine Ordner freigeschaltet.</div>
                    </t>
                    <div class="row g-3">
                        <t t-foreach="folders" t-as="folder">
                            <div class="col-12 col-md-6 col-lg-4">
                                <a t-att-href="'/media-approval/folder/%s' % folder.id" class="glma-folder-card">
                                    <span class="glma-folder-title" t-esc="folder.name"/>
                                    <span class="glma-folder-sub"><t t-esc="folder.file_count"/> Dateien</span>
                                </a>
                            </div>
                        </t>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="folder_detail" name="Medienfreigabe Ordnerdetail">
        <t t-call="website.layout">
            <div id="wrap" class="glma-page">
                <div class="container-fluid py-3">
                    <div class="d-flex justify-content-between align-items-center mb-3 px-lg-3">
                        <div>
                            <a href="/media-approval" class="glma-back">← Ordner</a>
                            <h1 class="mb-0" t-esc="folder.name"/>
                            <div class="text-muted">Angemeldet als <strong t-esc="person.name"/></div>
                        </div>
                        <a href="/media-approval/logout" class="btn btn-outline-secondary">Abmelden</a>
                    </div>
                    <div class="row g-3 px-lg-3">
                        <div class="col-12 col-lg-3 col-xl-2">
                            <div class="glma-list">
                                <t t-if="not files">
                                    <div class="p-3 text-muted">Keine Dateien in diesem Ordner.</div>
                                </t>
                                <t t-foreach="files" t-as="media">
                                    <a t-att-href="'/media-approval/folder/%s?file_id=%s' % (folder.id, media.id)"
                                       t-att-class="'glma-list-item %s %s' % (media.website_border_class, 'active' if selected and selected.id == media.id else '')">
                                        <span class="glma-file-name" t-esc="media.name"/>
                                        <span class="glma-file-meta">
                                            <t t-if="media.media_type == 'image'">Foto</t><t t-elif="media.media_type == 'video'">Video</t><t t-else="">Datei</t> ·
                                            <t t-esc="media.voted_count"/>/<t t-esc="media.required_count"/> Stimmen
                                        </span>
                                    </a>
                                </t>
                            </div>
                        </div>
                        <div class="col-12 col-lg-9 col-xl-10">
                            <t t-if="selected">
                                <t t-set="my_decision" t-value="selected.get_person_decision(person)"/>
                                <div t-att-class="'glma-preview-box %s' % selected.website_border_class">
                                    <div class="glma-preview-header">
                                        <div>
                                            <h2 t-esc="selected.name"/>
                                            <div class="text-muted">
                                                Status:
                                                <strong t-if="selected.decision_state == 'approved'">freigegeben</strong>
                                                <strong t-elif="selected.decision_state == 'rejected'">nicht freigegeben</strong>
                                                <strong t-else="">in Prüfung</strong>
                                                · <t t-esc="selected.voted_count"/>/<t t-esc="selected.required_count"/> Stimmen
                                            </div>
                                        </div>
                                    </div>
                                    <div class="glma-media-stage">
                                        <t t-if="selected.media_type == 'image'">
                                            <img t-att-src="'/media-approval/preview/%s' % selected.id" t-att-alt="selected.name" class="glma-preview-image"/>
                                        </t>
                                        <t t-elif="selected.media_type == 'video'">
                                            <video controls="controls" preload="metadata" class="glma-preview-video">
                                                <source t-att-src="'/media-approval/preview/%s' % selected.id" t-att-type="selected.mimetype"/>
                                                Dein Browser kann dieses Video nicht abspielen.
                                            </video>
                                        </t>
                                        <t t-else="">
                                            <div class="glma-generic-file">
                                                <div class="glma-generic-icon">📄</div>
                                                <div>Für diesen Dateityp gibt es keine Browser-Vorschau.</div>
                                            </div>
                                        </t>
                                    </div>
                                    <div class="glma-actions">
                                        <form method="post" t-att-action="'/media-approval/vote/%s' % selected.id">
                                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                            <input type="hidden" name="decision" value="approved"/>
                                            <button type="submit" t-att-class="'btn btn-success btn-lg %s' % ('glma-active-vote' if my_decision == 'approved' else '')">Freigeben</button>
                                        </form>
                                        <form method="post" t-att-action="'/media-approval/vote/%s' % selected.id">
                                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                            <input type="hidden" name="decision" value="rejected"/>
                                            <button type="submit" t-att-class="'btn btn-danger btn-lg %s' % ('glma-active-vote' if my_decision == 'rejected' else '')">Nicht freigeben</button>
                                        </form>
                                        <t t-if="selected.download_unlocked">
                                            <a t-att-href="'/media-approval/download/%s' % selected.id" class="btn btn-success btn-lg">Download</a>
                                        </t>
                                        <t t-else="">
                                            <button class="btn btn-secondary btn-lg" disabled="disabled">Download</button>
                                        </t>
                                    </div>
                                    <div class="glma-legend">
                                        <span><i class="glma-dot glma-dot-green"></i> Grün: alle haben freigegeben</span>
                                        <span><i class="glma-dot glma-dot-orange"></i> Orange: noch nicht abgeschlossen</span>
                                        <span><i class="glma-dot glma-dot-red"></i> Rot: vollständig bewertet, mindestens eine Ablehnung</span>
                                    </div>
                                </div>
                            </t>
                            <t t-else="">
                                <div class="alert alert-info">Bitte links eine Datei auswählen.</div>
                            </t>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="backend_upload_page" name="Medienfreigabe Backend Mehrfach-Upload">
        <t t-call="website.layout">
            <div id="wrap" class="glma-page glma-upload-page">
                <div class="container py-4">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <div>
                            <h1 class="mb-1">Dateien hochladen</h1>
                            <div class="text-muted">Mehrfach-Upload direkt in den ausgewählten Hetzner-Unterordner.</div>
                        </div>
                        <a href="/web#action=groundlift_media_approval.action_gl_media_folder" class="btn btn-outline-secondary">Zurück zu Odoo</a>
                    </div>
                    <div class="card shadow-sm">
                        <div class="card-body">
                            <input type="hidden" id="glma_csrf" t-att-value="request.csrf_token()"/>
                            <div class="row g-3 align-items-end">
                                <div class="col-12 col-lg-5">
                                    <label class="form-label" for="glma_folder_select">Unterordner</label>
                                    <select id="glma_folder_select" class="form-select form-select-lg">
                                        <option value="">Bitte auswählen</option>
                                        <t t-foreach="folders" t-as="folder">
                                            <option t-att-value="folder.id" t-att-selected="selected_folder and selected_folder.id == folder.id">
                                                <t t-esc="folder.name"/>
                                            </option>
                                        </t>
                                    </select>
                                </div>
                                <div class="col-12 col-lg-5">
                                    <label class="form-label" for="glma_file_input">Fotos und Videos</label>
                                    <input id="glma_file_input" class="form-control form-control-lg" type="file" multiple="multiple" accept="image/*,video/*"/>
                                </div>
                                <div class="col-12 col-lg-2 d-grid">
                                    <button id="glma_upload_start" type="button" class="btn btn-primary btn-lg">Upload starten</button>
                                </div>
                            </div>
                            <div class="small text-muted mt-3">
                                Maximal <t t-esc="max_files"/> Dateien pro Durchlauf, maximal <t t-esc="max_size_mb"/> MB pro Datei. Die Dateien werden einzeln übertragen und danach direkt auf den Hetzner-Server geschrieben.
                            </div>
                            <div class="progress mt-4" style="height: 24px;">
                                <div id="glma_upload_total_bar" class="progress-bar" role="progressbar" style="width: 0%;">0%</div>
                            </div>
                            <div id="glma_upload_status" class="alert alert-info mt-3 d-none"></div>
                            <div id="glma_upload_log" class="list-group mt-3"></div>
                        </div>
                    </div>
                </div>
            </div>
            <script type="text/javascript"><![CDATA[
(function () {
    const maxFiles = 50;
    const maxSize = 200 * 1024 * 1024;
    const parallelUploads = 2;
    const folderSelect = document.getElementById('glma_folder_select');
    const fileInput = document.getElementById('glma_file_input');
    const startButton = document.getElementById('glma_upload_start');
    const csrfInput = document.getElementById('glma_csrf');
    const log = document.getElementById('glma_upload_log');
    const statusBox = document.getElementById('glma_upload_status');
    const totalBar = document.getElementById('glma_upload_total_bar');

    function setStatus(message, kind) {
        statusBox.className = 'alert mt-3 alert-' + (kind || 'info');
        statusBox.textContent = message;
        statusBox.classList.remove('d-none');
    }

    function addLogItem(file) {
        const item = document.createElement('div');
        item.className = 'list-group-item';
        item.innerHTML = '<div class="d-flex justify-content-between"><strong></strong><span class="badge bg-secondary">wartet</span></div><div class="progress mt-2" style="height: 10px;"><div class="progress-bar" style="width:0%"></div></div><div class="small text-muted mt-1"></div>';
        item.querySelector('strong').textContent = file.name;
        log.appendChild(item);
        return item;
    }

    function setItem(item, percent, badge, badgeClass, detail) {
        const bar = item.querySelector('.progress-bar');
        const badgeNode = item.querySelector('.badge');
        const detailNode = item.querySelector('.small');
        bar.style.width = Math.max(0, Math.min(100, percent || 0)) + '%';
        badgeNode.className = 'badge ' + (badgeClass || 'bg-secondary');
        badgeNode.textContent = badge || 'wartet';
        detailNode.textContent = detail || '';
    }

    function uploadOne(file, item, folderId) {
        return new Promise((resolve) => {
            const xhr = new XMLHttpRequest();
            const form = new FormData();
            form.append('csrf_token', csrfInput.value);
            form.append('folder_id', folderId);
            form.append('file', file, file.name);
            xhr.open('POST', '/media-approval/backend/upload-file', true);
            xhr.upload.onprogress = function (event) {
                if (event.lengthComputable) {
                    setItem(item, Math.round((event.loaded / event.total) * 100), 'lädt hoch', 'bg-primary', Math.round(event.loaded / 1024 / 1024) + ' MB / ' + Math.round(event.total / 1024 / 1024) + ' MB');
                } else {
                    setItem(item, 10, 'lädt hoch', 'bg-primary', 'Upload läuft...');
                }
            };
            xhr.onload = function () {
                let payload = null;
                try { payload = JSON.parse(xhr.responseText || '{}'); } catch (e) {}
                if (xhr.status >= 200 && xhr.status < 300 && payload && payload.ok) {
                    setItem(item, 100, 'fertig', 'bg-success', 'Übertragen als: ' + payload.name);
                    resolve({ok: true});
                } else {
                    const error = payload && payload.error ? payload.error : ('HTTP ' + xhr.status);
                    setItem(item, 100, 'Fehler', 'bg-danger', error);
                    resolve({ok: false, error: error});
                }
            };
            xhr.onerror = function () {
                setItem(item, 100, 'Fehler', 'bg-danger', 'Netzwerkfehler beim Upload.');
                resolve({ok: false, error: 'Netzwerkfehler'});
            };
            setItem(item, 0, 'startet', 'bg-primary', 'Upload wird vorbereitet...');
            xhr.send(form);
        });
    }

    function updateTotal(done, total) {
        const percent = total ? Math.round((done / total) * 100) : 0;
        totalBar.style.width = percent + '%';
        totalBar.textContent = percent + '%';
    }

    startButton.addEventListener('click', async function () {
        const folderId = folderSelect.value;
        const files = Array.from(fileInput.files || []);
        log.innerHTML = '';
        updateTotal(0, 0);
        if (!folderId) {
            setStatus('Bitte zuerst einen Unterordner auswählen.', 'warning');
            return;
        }
        if (!files.length) {
            setStatus('Bitte mindestens eine Datei auswählen.', 'warning');
            return;
        }
        if (files.length > maxFiles) {
            setStatus('Bitte maximal ' + maxFiles + ' Dateien pro Durchlauf auswählen.', 'danger');
            return;
        }
        const tooLarge = files.filter((file) => file.size > maxSize);
        if (tooLarge.length) {
            setStatus('Mindestens eine Datei ist größer als 200 MB: ' + tooLarge.map((f) => f.name).join(', '), 'danger');
            return;
        }
        startButton.disabled = true;
        fileInput.disabled = true;
        folderSelect.disabled = true;
        setStatus('Upload läuft. Bitte dieses Fenster nicht schließen.', 'info');
        const queue = files.map((file) => ({file: file, item: addLogItem(file)}));
        let done = 0;
        let ok = 0;
        let failed = 0;
        updateTotal(0, queue.length);

        async function worker() {
            while (queue.length) {
                const next = queue.shift();
                const result = await uploadOne(next.file, next.item, folderId);
                done += 1;
                if (result.ok) { ok += 1; } else { failed += 1; }
                updateTotal(done, files.length);
            }
        }
        const workers = [];
        for (let i = 0; i < Math.min(parallelUploads, files.length); i++) {
            workers.push(worker());
        }
        await Promise.all(workers);
        startButton.disabled = false;
        fileInput.disabled = false;
        folderSelect.disabled = false;
        if (failed) {
            setStatus('Upload abgeschlossen: ' + ok + ' erfolgreich, ' + failed + ' fehlgeschlagen.', 'warning');
        } else {
            setStatus('Upload abgeschlossen: ' + ok + ' Dateien erfolgreich übertragen.', 'success');
        }
    });
})();
            ]]></script>
        </t>
    </template>

</odoo>
